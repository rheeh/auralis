# Auralis 产品范围

Auralis 是本地优先、面向个人创作者的短篇/单章 AI 广播剧工作台，沿用 SonicVale 的 Vue 3、FastAPI、SQLAlchemy、SQLite 和 FFmpeg。

## 制作主线

小说原文 → 人物草稿确认 → 台本生成与独立审查 → 用户确认 → 正式章节台词 → 配音 take → 素材与四轨时间线 → FFmpeg WAV。

草稿与正式工程分离。新建同名章节创建独立工程；覆盖已有章节属于另一个需要明确目标、版本和确认的 API 操作。已有 take 可试听和切换；旧输入的结果保留历史，但不能冒充新文字的配音。

## 当前能力边界

- 原文、人物、台本和表演设计可迭代；审查/返修失败从保存的 checkpoint 显式重试。
- 本地有限队列处理配音。进程中断标记待处理，不自动补发可能收费的请求。
- 生成人声、后期版本、素材文件保留独立路径。生成输入来源与当前适用性分别显示。
- 声音编排支持位置、裁剪、时长、音量、淡入淡出、静音、播放速率及素材循环。阶段成片列出缺失内容。
- `audioEvents` 中只有结构化素材编排事件直接影响时间线；自然语言声音说明需要手工选素材/编排，不自动解释为全部已执行效果。
- 公开 Demo 使用审定台本和预生成音频。修改文本不会自动调用 TTS，不能以样片证明实时生成效果。
- 无指纹的历史音频保持兼容，但其输入来源无法追溯验证。桌面壳保留，浏览器工作台是本轮主要验收路径。

## 非目标

不改技术栈，不换 Agent/workflow 框架，不引入微服务、分布式队列、账号、支付、多人协作、RAG 或通用插件系统。不承诺生成质量、听感或商业指标提升。

[本地运行](README.md#本地运行) · [当前架构](docs/project-map.md) · [验收与剩余项](BACKLOG.md)
