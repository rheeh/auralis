# 连贯对白的默认指导调整

2026-09-26。用户试听《最后一盏灯》后指出逐字咬字、句内停顿和收尾过于明显。历史请求显示，样片本句指导要求“翻找动作处停一下”，通用 TTS 指导还要求一处重音或句尾变化；编剧默认规则也鼓励每句安排可听变化。这些是提示词中的已确认要求，不能据此断定听感问题只有一个原因。

## 实现

- 共享编剧规范改为按短语连贯表达，声音指导优先说明说话目的。普通对白的 `productionNote` 和特殊表演 `performanceCue.delivery` 可以留空；省略号、特殊发声和停顿需有情节或用户依据。生成、独立审查、返修及兼容改编入口沿用共享规则。
- 原生 TTS 的默认微弱、稍弱、中等指导不再强制关键词着力、重音、句尾收住或回到平稳。强烈表演及用户明确填写的指导仍保留；Edge 的参数映射未改动。
- 配音请求不重复发送已包含在本句指导、共同基调或声线中的相同场景指导。不同的本句意图仍发送，完整场景设计仍保存在 trace；不改写朗读正文和已有台本。
- 请求策略版本更新为 `speech-context-2026-09-26-v3`。旧 take 保留；输入指纹可能显示已变化，后续显式重新生成才会应用新的请求。

## 验证

先添加回归用例，旧实现出现 14 个断言失败，涵盖默认字词表演、重复指导和编剧规则。修改后针对性 57 项测试通过。

`./scripts/verify.sh` 通过：后端 226 项、API smoke、前端 39 项、Electron 语法检查、普通构建和静态 Demo 构建。测试使用临时配置和数据库，禁止外部网络，不读取真实生产数据。

确认本地配音、助手和改编均空闲后，重启当前仓库后端。启动器在资源读取阶段触发三秒超时；后续独立读取 health、项目、素材和队列均成功，配音预览返回 v3 新指导，重复本句要求由两次变为一次。没有为此次核对提交生成任务。

## 第一批验证边界

本次没有调用真实 LLM/TTS，没有重生成或重渲染旧样片，也没有替换公开 Demo。工程检查只能证明请求按规则构造，不能证明咬字、自然度或听感已经改善。已有台本保存的具体停顿要求仍需编辑或返修；旧样片中已报告的裁切与混音问题属于后续音频修订。

## 第二批：用户授权的局部重配与素材修订

用户要求只重做明显断字的几句，保留在工作台可见的版本，并补充素材。操作对象为项目 14 / 章节 10，生成前核实 Qwen-Audio 3.0 Plus 免费余额 8.06K、用完即停已开启；只提交三句，不调用其他模型或评估接口。生成后余额 7.97K，用完即停仍开启。

- 台词 257、259、262 使用更简短的口语指导；257、262 的首句句号改为逗号，259 去掉省略号。三句均形成第二个生成版本。
- 原来的 27 句保留，272 恢复原始“抽屉？”录音；错误裁切生成的处理版保留在历史中，未选用。
- 工作台原来不能将输入已变化的历史 take 显式用于当前台本。新增可见选用按钮，并在原有版本切换命令中记录 `take_selection` 事件，绑定当前请求指纹和实际源版本 ID。原始 take 的文本、快照、指纹不变；输入再次变化或文件回退到其他版本时，旧选用记录不自动适用。
- 未改变未知来源的旧音频兼容规则。先添加两项失败测试，再通过选用、变更失效、缺失文件回退和真实临时音频渲染检查。
- 第二批 `./scripts/verify.sh` 通过：228 后端测试、39 前端测试、API smoke、普通及 Demo 构建。测试全程使用临时配置与禁网环境。生产修订单独通过正式 HTTP 命令执行。
- 五份新素材经素材库导入，九处素材经插入接口加入章节。时间线刷新保留手工片段，然后按新的对白时长移动后续片段，保存音量与淡化。
- 成片 108.130 秒，30 句对白、20 项音效/BGM，50/50 片段，`is_partial=false`，无缺失台词。WAV 峰值 -1.43 dBFS，MP3 峰值 -1.40 dBFS，均未削波。原导出和原录音历史均保留。
- 浏览器验证工作台显示音频 30/30、20 项素材，259 的版本菜单有 1/2 和 2/2；原录音显示“已选用历史录音用于当前台本”。

句内静音测量以 10 ms 窗口、-45 dBFS 阈值统计，259 从 5.496 秒变为 2.304 秒，原两段超过 0.6 秒的内部静音消失；257 从 5.664 秒变为 3.504 秒；262 从 6.216 秒变为 4.848 秒。此结果只说明时间结构变化，不能单独证明自然度、文本完整或声线一致。当前工具不能提供模型实际听音判断，最终听感仍待用户试听；未替换公开 Demo。

生产回执、旧编排快照、历史混音 manifest 与本次验证保存在被忽略的 `.local-data/projects/14/10/revision-2/`；成片导出保存在 `output/last-light-20260926/revision-2/`，不提交用户音频与账户资料。
